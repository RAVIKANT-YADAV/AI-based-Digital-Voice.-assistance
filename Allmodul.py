import sys
import os
import datetime
import time
import pyttsx3
import datetime
import wikipedia
import speech_recognition as sr
import takeCommand
import speak_Function
import webbrowser as web
import pkg_resources

#whatsapp use moduls

#use in check internet conn file


def allpackages():
   installed_packages = pkg_resources.working_set
   installed_packages_list = sorted(["%s==%s" % (i.key, i.version)
   for i in installed_packages_list:
      print(installed_packages_list)

if __name__ == "__main__":
       # sf.speak("THis function is make for  All modul import ! ")
       allpackages()